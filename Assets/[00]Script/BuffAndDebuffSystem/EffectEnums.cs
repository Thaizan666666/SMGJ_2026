using UnityEngine;

public class EffectEnums 
{
    public enum TimedStatType { Speed, HeatIncreaseRate }
    public enum InstantStatType { Speed, HeatGauge }
    public enum ModifierMode { Add, Subtract }
}
